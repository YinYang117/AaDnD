import bcrypt from "bcrypt";
import jwt from "jsonwebtoken";
import User from "../models/User.js";

/* Register / Signup User */
export const register = async (req, res) => {
	try {
		const { name, gamertag, email, password, userAvatar}
	} catch (e) {

	}
}
